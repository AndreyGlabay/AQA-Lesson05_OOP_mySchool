package com.example.school.tests.api.dto; // (step 1.l) Create new package "dto";

public class Authors { // (step 1.m) In the "dto" package: create new java-class "Authors" for make DTO for the entity;
    int id; // (step 1.n) implement var according to the Authors - GET -JSON scheme;
    int idBook; // (step 1.n) implement var according to the Authors - GET -JSON scheme;
    String firstName; // (step 1.n) implement var according to the Authors - GET -JSON scheme;
    String lastName; // (step 1.n) implement var according to the Authors - GET -JSON scheme;
}
